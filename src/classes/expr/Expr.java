package classes.expr;

import classes.Part;
import org.objectweb.asm.ClassVisitor;
import org.objectweb.asm.MethodVisitor;

public class Expr implements Part {
    @Override
    public void compile(MethodVisitor mv, ClassVisitor cv) {
        //TODO
    }
}
